#!/bin/bash

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

while true; do
  clear
  "$SCRIPT_DIR/aicheck"
  echo
  echo "----------------------------------------"
  echo "Auto-refresh in 30s | Press Ctrl+C to exit"
  echo "----------------------------------------"
  sleep 30
done
